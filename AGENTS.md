# Repository instructions

- Treat `Phase/` and explicit user instructions as the scope authority.
- After every logical code change, update the repository-root `READ.md` in the same change set with affected phases, tests and results, unverified work, percentage changes, and the next action.
- Never claim hardware or central-service verification without recorded evidence from the actual Raspberry Pi and event network.

