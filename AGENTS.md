# Repository instructions

- Treat `Phase/` and explicit user instructions as the scope authority.
- After every logical code change, update the repository-root `READ.md` in the same change set with affected phases, checks and results, unverified work, percentage changes, and the next action.
- Never claim hardware, Raspberry Pi, or event-service verification without recorded evidence from the actual device or service.
